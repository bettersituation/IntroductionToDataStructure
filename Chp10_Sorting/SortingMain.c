#include "Sorting.h"
#include "Priority.h"


int main(void)
{
	int arr[6] = { 3, 2, 4, 1, 7, 6 };
	int n = 6;
	
	PrintArray(arr, n);
	
	//BubbleSort(arr, n, LessPriority);
	//PrintArray(arr, n);

	//SelectionSort(arr, n, LessPriority);
	//PrintArray(arr, n);

	//InsertionSort(arr, n, LessPriority);
	//PrintArray(arr, n);

	//HeapSort(arr, n, LessPriority);
	//PrintArray(arr, n);

	//MergeSort(arr, n, LessPriority);
	//PrintArray(arr, n);

	//QuickSort(arr, n, LessPriority);
	//PrintArray(arr, n);

	//RadixSort(arr, n, LessPriority);
	//PrintArray(arr, n);
}
